<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.CryptoXMLProjectNew" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:statusBarColor">@color/main_background</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>


    <style name="RoundedImageView">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopRight">8dp</item>
        <item name="cornerSizeTopLeft">8dp</item>
        <item name="cornerSizeBottomRight">8dp</item>
        <item name="cornerSizeBottomLeft">8dp</item>
    </style>

    <style name="RadioButtonStyle">
        <item name="buttonTint">@color/main_text_color</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/main_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/im_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.CryptoXMLProjectNew</item>
    </style>
</resources>